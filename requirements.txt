# Application runtime (app.py / predictor.py)
numpy>=1.26
pandas>=2.0
scikit-learn>=1.9  # serving bundle was trained on 1.9.0; older versions emit InconsistentVersionWarning
scipy>=1.10
joblib>=1.3
streamlit>=1.40
plotly>=5.18

# Required only to regenerate the serving bundle (train_export.py)
datasets>=2.19

# Required only for the research notebook (transactionbert.ipynb)
# torch, transformers, xgboost, lightgbm, matplotlib
